// import { Page } from "@/app/model/common/page.model"
import { AppDispatch } from "@/lib/store"
import { RoomModel } from "@/app/model/room/room.model"

// const totalElements = async (rooms: Page<RoomModel>, dispatch: AppDispatch) => {
//   return rooms.totalElements
// }

// const totalPages = async (rooms: Page<RoomModel>, dispatch: AppDispatch) => {
//   return rooms.totalPages
// }

// const totalSize = async (rooms: Page<RoomModel>, dispatch: AppDispatch) => {
//   return rooms.size
// }

// export const PageService = {
//   totalElements,
//   totalPages,
//   totalSize
// }