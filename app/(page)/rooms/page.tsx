import SideBar from "@/app/components/common/SideBar";
import { RoomModel } from "@/app/model/room/room";
export default function Room() {
  const resp = Response.json

  return (
    <div>
      <SideBar/>
    </div>
  );
}
